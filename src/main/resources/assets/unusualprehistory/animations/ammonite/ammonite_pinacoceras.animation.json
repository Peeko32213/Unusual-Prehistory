{
	"format_version": "1.8.0",
	"animations": {
		"animation.pinacoceras.swim": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Shell": {
					"rotation": [
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1)))*5",
						0,
						"math.sin(query.anim_time*(90+90*math.clamp(query.ground_speed,0,1))-120)*-2.5"
					],
					"position": [
						"math.sin(query.anim_time*(90+90*math.clamp(query.ground_speed,0,1)))*0.5",
						"2+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1))+90)*-1",
						"math.sin(query.anim_time*(45+45*math.clamp(query.ground_speed,0,1)))*1"
					]
				},
				"Head": {
					"rotation": ["math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1))+60)*-2.5", 0, 0],
					"scale": [
						"1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-0.1",
						"1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-0.1",
						"1.1+0.2*math.clamp(query.ground_speed,0,1)+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+60)*0.15"
					]
				},
				"EyeLeft": {
					"rotation": [0, 0, "10+math.sin(query.anim_time*90-120)*2.5+math.sin(query.anim_time*45)*10"]
				},
				"EyeRight": {
					"rotation": [0, 0, "-10+math.sin(query.anim_time*90-120)*2.5+math.sin(query.anim_time*45+90)*-10"]
				},
				"Arm3": {
					"rotation": [
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm2": {
					"rotation": [
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm1": {
					"rotation": ["-15+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*20", 0, 0],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm4": {
					"rotation": [
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm5": {
					"rotation": [
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm6": {
					"rotation": ["15+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-20", 0, 0],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Mouth": {
					"scale": [1, 1, "1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+60)*0.1"]
				}
			}
		},
		"animation.pinacoceras.hidden": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Shell": {
					"rotation": ["10+math.sin(query.anim_time*180)*2.5", 0, "math.sin(query.anim_time*90-120)*-2.5"],
					"position": ["math.sin(query.anim_time*90)*0.5", "2+math.sin(query.anim_time*180+90)*-0.5", "math.sin(query.anim_time*45)*0.5"]
				},
				"Head": {
					"position": [0, -2, 2],
					"scale": [0.1, 1, 1]
				},
				"EyeLeft": {
					"rotation": ["math.sin(query.anim_time*90)*-10", 0, "22.5+math.sin(query.anim_time*45)*22.5"],
					"position": {
						"0.0": [0, "-1.5+math.sin(query.anim_time*90)*1.5", -1],
						"2.0": [0, -3, -1],
						"4.0": [0, -3, -1],
						"6.0": [0, "-1.5+math.sin(query.anim_time*90)*1.5", -1],
						"8.0": [0, "-1.5+math.sin(query.anim_time*90)*1.5", -1]
					}
				},
				"EyeRight": {
					"rotation": ["math.sin(query.anim_time*90)*10", 0, "-22.5+math.sin(query.anim_time*45)*-22.5"],
					"position": {
						"0.0": [0, "-1.5+math.sin(query.anim_time*90-90)*-1.5", -1],
						"2.0": [0, -3, -1],
						"4.0": [0, -3, -1],
						"6.0": [0, "-1.5+math.sin(query.anim_time*90-90)*-1.5", -1],
						"8.0": [0, "-1.5+math.sin(query.anim_time*90-90)*-1.5", -1]
					}
				}
			}
		},
		"animation.pinacoceras.flop": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Shell": {
					"rotation": [
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1)))*5",
						0,
						"math.sin(query.anim_time*(90+90*math.clamp(query.ground_speed,0,1))-120)*-2.5"
					],
					"position": [
						"math.sin(query.anim_time*(90+90*math.clamp(query.ground_speed,0,1)))*0.5",
						"2+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1))+90)*-1",
						"math.sin(query.anim_time*(45+45*math.clamp(query.ground_speed,0,1)))*1"
					]
				},
				"Head": {
					"rotation": ["math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed,0,1))+60)*-2.5", 0, 0],
					"scale": [
						"1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-0.1",
						"1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-0.1",
						"1.1+0.2*math.clamp(query.ground_speed,0,1)+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+60)*0.15"
					]
				},
				"EyeLeft": {
					"rotation": [0, 0, "10+math.sin(query.anim_time*90-120)*2.5+math.sin(query.anim_time*45)*10"]
				},
				"EyeRight": {
					"rotation": [0, 0, "-10+math.sin(query.anim_time*90-120)*2.5+math.sin(query.anim_time*45+90)*-10"]
				},
				"Arm3": {
					"rotation": [
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm2": {
					"rotation": [
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm1": {
					"rotation": ["-15+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*20", 0, 0],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm4": {
					"rotation": [
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						"-5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm5": {
					"rotation": [
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						"5+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-10",
						0
					],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Arm6": {
					"rotation": ["15+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1)))*-20", 0, 0],
					"scale": [1, 1, "1.2+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+120)*0.2"]
				},
				"Mouth": {
					"scale": [1, 1, "1.1+math.sin(query.anim_time*(360+360*math.clamp(query.ground_speed,0,1))+60)*0.1"]
				}
			}
		}
	}
}